apiVersion: 1

providers:
  - name: 'Address Parser'
    orgId: 1
    folder: 'Address Parser'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
